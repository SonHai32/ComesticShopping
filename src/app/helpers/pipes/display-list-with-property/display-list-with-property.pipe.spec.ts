import { DisplayListWithPropertyPipe } from './display-list-with-property.pipe';

describe('DisplayListWithPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayListWithPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
